README.md

## Descripción
Notas sobre mis primeras semanas en la clase de programación utilizando Python

## Temas cubiertos
**Variables y Entrada de Datos:**
* Declaración y asignación de variables.
* Captura de entrada del usuario mediante `input()`.

**Conversión de Tipos (Casting) y Concatenación:**
* Conversión de tipos de datos (`int()`, `float()`, `str()`).
* Combinación de texto y variables mediante concatenación y formato.

**Estructuras Condicionales y Control de Flujo:**
* Uso de sentencias `if`, `elif` y `else`.
* Operadores de comparación (`==`, `!=`, `<`, `>`, `<=`, `>=`).
* Operadores booleanos y de lógica (`and`, `or`, `not`).
# Oliver Zavala 
# fecha 09/22/2026